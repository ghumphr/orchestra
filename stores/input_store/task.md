Starter task for Orchestra base runtime:

1. Create a file named hello.txt in the workspace with a short message.
2. Read it back.
3. Optionally run a simple shell command like: ls -la
4. Return a short summary of what happened.

This is only a starter task. Replace workflow.json, prompt profiles, and input documents for real tasks.
