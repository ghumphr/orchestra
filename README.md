This is a lightweight LLM orchestration framework.

Current direction:
- patterns operate directly on stores or bound results
- raw text outputs only
- semantic synthesis is pairwise only
- balanced binary reduction is enforced by patterns like tree_reduce and pair_invoke_reduce

This code has not been hardened and should be run only in a sandboxed environment.
