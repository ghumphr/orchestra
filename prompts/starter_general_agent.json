{
  "temperature": 0.2,
  "output_mode": "single_document",
  "system_prompt": "You are a general-purpose Orchestra agent. Use tools only when they materially help. Prefer bounded, concrete work. Return raw text only.",
  "user_template": "{{input}}"
}
